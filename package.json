{
  "name": "knomera-internal",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:worker": "opennextjs-cloudflare build",
    "start": "next start",
    "lint": "eslint",
    "db:schema": "node --env-file=.env.local --import tsx scripts/apply-schema.ts",
    "db:migrate": "node --env-file=.env.local --import tsx scripts/migrate.ts",
    "db:seed": "node --env-file=.env.local --import tsx scripts/seed.ts",
    "db:seed:stage2": "node --env-file=.env.local --import tsx scripts/seed-stage2.ts",
    "db:setup": "npm run db:schema && npm run db:migrate && npm run db:seed && npm run db:seed:stage2",
    "hash-password": "node --import tsx scripts/hash-password.ts",
    "smoke": "node --env-file=.env.local --import tsx scripts/smoke-test.ts",
    "preview": "npm run build:worker && opennextjs-cloudflare preview",
    "deploy": "npm run build:worker && opennextjs-cloudflare deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareEnv cloudflare-env.d.ts"
  },
  "dependencies": {
    "@opennextjs/cloudflare": "^1.11.0",
    "bcryptjs": "^3.0.2",
    "jose": "^6.0.11",
    "lucide-react": "^0.511.0",
    "next": "16.3.6",
    "postgres": "^3.4.7",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.6",
    "tailwindcss": "^4",
    "tsx": "^4.19.4",
    "typescript": "^5",
    "wrangler": "^4.20.0"
  }
}
